╔════════════════════════════════════════════════════════════════╗
║           DEPLOY ILS 2.0 TO RAILWAY - QUICK GUIDE            ║
╚════════════════════════════════════════════════════════════════╝

🚀 FASTEST PATH: Use Railway Web Interface + GitHub

STEP 1: Setup Railway Project (5 minutes)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Go to: https://railway.app/new
2. Click "Deploy from GitHub repo"
3. Select your ILS2.0 repository
4. Railway auto-detects and starts building ✅

STEP 2: Add Databases (2 minutes)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
In your Railway project:

PostgreSQL:
  • Click "+ New" → "Database" → "PostgreSQL"
  • Railway auto-sets DATABASE_URL ✅

Redis:
  • Click "+ New" → "Database" → "Redis"  
  • Railway auto-sets REDIS_URL ✅

STEP 3: Set Environment Variables (3 minutes)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Click your app → "Variables" → Add:

Required:
  NODE_ENV=production
  PORT=5000
  SESSION_SECRET=<run: openssl rand -base64 48>
  ADMIN_SETUP_KEY=<run: openssl rand -base64 24>

Master User:
  MASTER_USER_EMAIL=admin@yourdomain.com
  MASTER_USER_PASSWORD=<strong-password-12+chars>
  MASTER_USER_FIRST_NAME=Admin
  MASTER_USER_LAST_NAME=User
  MASTER_USER_ORGANIZATION=ILS Platform

STEP 4: Deploy (Automatic!)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Railway automatically deploys! Watch in "Deployments" tab.

STEP 5: Initialize Database (1 minute)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
In Railway → Your App → "Shell" tab:

  npm run db:push

This creates all 90+ database tables.

STEP 6: Verify (1 minute)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Your app → "Settings" → "Domains" → Click the URL

Check health:
  https://your-app.railway.app/api/health

Should show:
  {"status":"ok","database":"connected","redis":"connected"}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📱 ALTERNATIVE: Use Railway CLI from YOUR Terminal

Since I can't open browsers, YOU can run these in your terminal:

# Step 1: Login
npx @railway/cli login

# Step 2: Link/Create Project  
npx @railway/cli init

# Step 3: Deploy
npx @railway/cli up

# Step 4: Set Variables
npx @railway/cli variables set NODE_ENV=production
npx @railway/cli variables set PORT=5000
npx @railway/cli variables set SESSION_SECRET="$(openssl rand -base64 48)"
npx @railway/cli variables set ADMIN_SETUP_KEY="$(openssl rand -base64 24)"

# Step 5: Initialize DB
npx @railway/cli run npm run db:push

# Step 6: Check Status
npx @railway/cli status
npx @railway/cli logs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION CREATED:

✅ deploy-railway.sh          - Automated deployment script
✅ DEPLOY_NOW.md              - Step-by-step deployment guide
✅ RAILWAY_DEPLOYMENT_GUIDE.md - Complete reference (17 sections)
✅ RAILWAY_QUICK_START.md     - 15-minute quick start
✅ DEPLOYMENT_CHECKLIST.md    - Verification checklist

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 RECOMMENDED: Use Railway Web Interface (Easiest!)
   It's visual, intuitive, and doesn't require CLI setup.

Total Time: ~15 minutes
Status: READY TO DEPLOY ✅

