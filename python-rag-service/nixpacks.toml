# Nixpacks Configuration for Python RAG Service
[phases.setup]
nixPkgs = ["python311", "gcc", "zlib", "glib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"

[variables]
PYTHON_ENV = "production"
