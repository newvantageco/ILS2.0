# ILS 2.0 - Python RAG Service Dependencies
# Railway-optimized configuration (CPU-only, memory efficient)
# Updated: November 2025

# FastAPI and web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.1
python-multipart>=0.0.12

# PostgreSQL & pgvector
psycopg2-binary>=2.9.9
pgvector>=0.3.5

# Embeddings & NLP - CPU-only for Railway
# Note: Using CPU-only wheel to reduce memory usage
sentence-transformers>=3.0.0
# torch is auto-installed by sentence-transformers

# OpenAI (for fallback/hybrid embeddings)
openai>=1.50.0

# Utilities
numpy>=1.26.0,<2.0.0
httpx>=0.27.0

# Logging
loguru>=0.7.2

# Testing (optional, not needed in production)
pytest>=8.0.0
pytest-asyncio>=0.23.0
