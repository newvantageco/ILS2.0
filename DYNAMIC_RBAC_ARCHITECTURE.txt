# 🏗️ Dynamic RBAC System Architecture

## Visual Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         DYNAMIC RBAC SYSTEM                             │
│                    "World-Class, Database-Driven"                       │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                           📊 DATABASE LAYER                             │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌──────────────────────┐
                    │  permission_categories│
                    │ ────────────────────  │
                    │  id, name, order     │
                    └──────────┬───────────┘
                              │
                              ▼
                    ┌──────────────────────┐
                    │    permissions       │  ⭐ MASTER CATALOG (70+)
                    │ ──────────────────── │
                    │  id, permission_key  │
                    │  plan_level          │  🔑 Feature Gating
                    │  category_id         │
                    └──────────┬───────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
    ┌─────────────────┐  ┌─────────────────┐  ┌──────────────────┐
    │ dynamic_roles   │  │role_permissions │  │user_dynamic_roles│
    │ ─────────────── │  │─────────────────│  │──────────────────│
    │ id, company_id  │◄─┤ role_id         │  │ user_id          │
    │ name            │  │ permission_id   ├─►│ role_id          │
    │ is_deletable    │  └─────────────────┘  │ is_primary       │
    └─────────────────┘                       └──────────────────┘
           │                                           ▲
           │ 🏢 Company-Specific                      │
           │    (Multi-Tenant)                        │
           │                                          │
           └──────────────────────────────────────────┘
                          Many-to-Many

┌─────────────────────────────────────────────────────────────────────────┐
│                        🔐 PERMISSION FLOW                               │
└─────────────────────────────────────────────────────────────────────────┘

1. USER LOGS IN
   ↓
   ┌──────────────────────────────────────────────────────────┐
   │ DynamicPermissionService.getUserPermissions(userId)      │
   └──────────────────────────────────────────────────────────┘
   ↓
2. FETCH ALL ROLES
   ↓
   ┌──────────────────────────────────────────────────────────┐
   │ SELECT roles FROM user_dynamic_roles WHERE user_id = ?  │
   │ Result: ['Admin', 'ECP']                                │
   └──────────────────────────────────────────────────────────┘
   ↓
3. GET UNION OF PERMISSIONS
   ↓
   ┌──────────────────────────────────────────────────────────┐
   │ SELECT DISTINCT permissions                             │
   │ FROM role_permissions                                   │
   │ WHERE role_id IN ('admin-id', 'ecp-id')                │
   │                                                         │
   │ Result: ['orders:create', 'users:manage',              │
   │          'patients:view', 'analytics:view', ...]       │
   └──────────────────────────────────────────────────────────┘
   ↓
4. FILTER BY SUBSCRIPTION PLAN 🎯
   ↓
   ┌──────────────────────────────────────────────────────────┐
   │ if (company.plan === 'free') {                          │
   │   filter out permissions where plan_level != 'free'     │
   │ }                                                       │
   │                                                         │
   │ BEFORE: ['orders:create', 'analytics:view', ...]       │
   │ AFTER:  ['orders:create', ...]  (analytics removed!)   │
   └──────────────────────────────────────────────────────────┘
   ↓
5. CACHE IN SESSION 🚀
   ↓
   ┌──────────────────────────────────────────────────────────┐
   │ session.cached_permissions = {                          │
   │   permissions: ['orders:create', ...],                  │
   │   rolePermissions: ['orders:create', 'analytics:view'],│
   │   isOwner: false,                                       │
   │   subscriptionPlan: 'free'                             │
   │ }                                                       │
   └──────────────────────────────────────────────────────────┘
   ↓
6. ATTACH TO REQUEST
   ↓
   ┌──────────────────────────────────────────────────────────┐
   │ req.user.permissions = session.cached_permissions       │
   └──────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                      🛡️ MIDDLEWARE FLOW                                 │
└─────────────────────────────────────────────────────────────────────────┘

REQUEST: POST /api/orders
   ↓
┌──────────────────────────────────────────────────────────┐
│ authenticateUser(req, res, next)                        │
│ → Verify token, load user                              │
└──────────────────────────────────────────────────────────┘
   ↓
┌──────────────────────────────────────────────────────────┐
│ attachPermissions(req, res, next)                       │
│ → Load cached permissions into req.user                 │
│ → req.user.permissions = [...]                         │
└──────────────────────────────────────────────────────────┘
   ↓
┌──────────────────────────────────────────────────────────┐
│ requirePermission('orders:create')(req, res, next)      │
│                                                         │
│ ✅ if (req.user.permissions.includes('orders:create'))│
│    → next() // Allow!                                  │
│                                                         │
│ ❌ else                                                │
│    → 403 Forbidden                                     │
└──────────────────────────────────────────────────────────┘
   ↓
   ROUTE HANDLER: createOrder()

Performance: <1ms (array lookup, NO DATABASE QUERY!)

┌─────────────────────────────────────────────────────────────────────────┐
│                    💰 FEATURE GATING FLOW                               │
└─────────────────────────────────────────────────────────────────────────┘

USER HAS ADMIN ROLE → includes 'analytics:view' permission
                       BUT
COMPANY IS ON FREE PLAN → plan_level = 'free'
                       ↓
        ┌───────────────────────────────┐
        │ Filter at Login:              │
        │                               │
        │ if (perm.plan_level === 'add_on_analytics' &&  │
        │     company.plan !== 'add_on_analytics')       │
        │   → EXCLUDE from sessionPermissions            │
        └───────────────────────────────┘
                       ↓
RESULT:
  • user.rolePermissions = ['analytics:view', ...]  ✅ Has in role
  • user.sessionPermissions = []  ❌ Not in session (filtered out)
                       ↓
        ┌───────────────────────────────┐
        │ SMART UPSELL UI:              │
        │                               │
        │ if (rolePermissions.includes('analytics:view') && │
        │     !sessionPermissions.includes('analytics:view'))│
        │   → Show locked badge         │
        │   → Show "Upgrade to Analytics Add-On" button     │
        └───────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    🏢 MULTI-TENANCY ISOLATION                           │
└─────────────────────────────────────────────────────────────────────────┘

                    COMPANY A                     COMPANY B
                    ─────────                     ─────────
                        │                             │
          ┌─────────────┼─────────────┐              │
          ▼             ▼             ▼              ▼
    ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
    │  Admin  │   │   ECP   │   │ Senior  │   │  Admin  │
    │  Role   │   │  Role   │   │  ECP    │   │  Role   │
    │         │   │         │   │ (Custom)│   │         │
    └─────────┘   └─────────┘   └─────────┘   └─────────┘
    company_id:     company_id:   company_id:   company_id:
      'comp-a'        'comp-a'      'comp-a'      'comp-b'

    ✅ Company A sees: [Admin, ECP, Senior ECP]
    ✅ Company B sees: [Admin, ECP, Lab Tech, ...]
    ❌ Company B CANNOT see "Senior ECP" (belongs to Company A)

    ALL QUERIES INCLUDE: WHERE company_id = :currentUserCompanyId

┌─────────────────────────────────────────────────────────────────────────┐
│                    👤 USER PERMISSION RESOLUTION                        │
└─────────────────────────────────────────────────────────────────────────┘

SCENARIO: User has BOTH "Admin" AND "ECP" roles

┌──────────────────┐         ┌──────────────────┐
│   Admin Role     │         │    ECP Role      │
│ ──────────────── │         │ ──────────────── │
│ • users:manage   │         │ • orders:create  │
│ • company:edit   │   +     │ • patients:view  │
│ • analytics:view │         │ • ai:basic       │
│ • ai:full        │         │                  │
└──────────────────┘         └──────────────────┘
          │                           │
          └───────────┬───────────────┘
                     ▼
            ┌─────────────────────┐
            │  SUM OF PERMISSIONS │
            │  (Union / Set.merge)│
            └─────────────────────┘
                     ▼
       ┌───────────────────────────┐
       │ Final User Permissions:   │
       │ • users:manage            │
       │ • company:edit            │
       │ • analytics:view          │
       │ • ai:full ✅ (highest)    │
       │ • orders:create           │
       │ • patients:view           │
       └───────────────────────────┘

NO MORE SWITCHING! User can do EVERYTHING they're allowed to do.

┌─────────────────────────────────────────────────────────────────────────┐
│                    🔄 ROLE MANAGEMENT WORKFLOW                          │
└─────────────────────────────────────────────────────────────────────────┘

ADMIN USER CREATES NEW ROLE:

1. User clicks "Create New Role" in UI
   ↓
2. Modal opens: Name + Description
   ↓
3. POST /api/roles
   {
     name: "Junior Optometrist",
     description: "Limited clinical access"
   }
   ↓
4. Backend creates role:
   INSERT INTO dynamic_roles (company_id, name, ...)
   VALUES (:companyId, 'Junior Optometrist', ...)
   ↓
5. User clicks "Edit" → Permission editor opens
   ↓
6. User checks permissions:
   ☑ patients:view
   ☑ examinations:view
   ☐ prescriptions:sign (unchecked)
   ↓
7. PUT /api/roles/:roleId
   { permissionIds: ['perm-1', 'perm-2', ...] }
   ↓
8. Backend updates:
   DELETE FROM dynamic_role_permissions WHERE role_id = :roleId
   INSERT INTO dynamic_role_permissions (role_id, permission_id)
   VALUES (:roleId, 'perm-1'), (:roleId, 'perm-2'), ...
   ↓
9. INVALIDATE CACHE for all users with this role
   UPDATE sessions SET cached_permissions = NULL
   WHERE user_id IN (SELECT user_id FROM user_dynamic_roles WHERE role_id = :roleId)
   ↓
10. ✅ Role updated! Users will get new permissions on next request.

┌─────────────────────────────────────────────────────────────────────────┐
│                    🎨 FRONTEND INTEGRATION                              │
└─────────────────────────────────────────────────────────────────────────┘

useUser Hook:
┌──────────────────────────────────────────────────────────┐
│ const user = useUser();                                  │
│                                                          │
│ user.permissions         // ['orders:create', ...]      │
│ user.rolePermissions     // ['analytics:view', ...]     │
│ user.roles               // [{ name: 'Admin', ... }]    │
│ user.isOwner             // false                       │
│ user.subscriptionPlan    // 'free'                      │
│                                                          │
│ // Helper methods:                                      │
│ user.hasPermission('orders:create')  // true           │
│ user.isFeatureLocked('analytics:view') // true!        │
└──────────────────────────────────────────────────────────┘

Conditional Rendering:
┌──────────────────────────────────────────────────────────┐
│ {user.hasPermission('users:manage_roles') && (          │
│   <Button>Manage Team</Button>                          │
│ )}                                                       │
│                                                          │
│ {user.isFeatureLocked('analytics:view') && (            │
│   <LockedFeatureBadge                                   │
│     message="Upgrade to Analytics Add-On"              │
│     onUpgrade={() => navigate('/billing')}             │
│   />                                                     │
│ )}                                                       │
└──────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    📊 PERFORMANCE CHARACTERISTICS                       │
└─────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────┬──────────────┬────────────────┐
│ Operation                 │ DB Queries   │ Response Time  │
├───────────────────────────┼──────────────┼────────────────┤
│ Permission Check          │ 0 (cached)   │ <1ms          │
│ User Login                │ 3            │ 10-50ms       │
│ Get Role Details          │ 3            │ 20-60ms       │
│ Create Role               │ 2            │ 10-30ms       │
│ Update Role Permissions   │ N+2          │ 50-200ms      │
│ Assign Role to User       │ 2            │ 10-30ms       │
└───────────────────────────┴──────────────┴────────────────┘

Cache Strategy:
• Permissions cached in session on login
• Invalidated on role/permission changes
• No external cache (Redis) required
• Uses PostgreSQL session store

Indexes:
• user_dynamic_roles(user_id)
• dynamic_role_permissions(role_id)
• permissions(permission_key)
• dynamic_roles(company_id, name)

┌─────────────────────────────────────────────────────────────────────────┐
│                    🔍 DEBUGGING & TROUBLESHOOTING                       │
└─────────────────────────────────────────────────────────────────────────┘

Check User's Permissions:
┌──────────────────────────────────────────────────────────┐
│ SELECT * FROM user_effective_permissions                │
│ WHERE user_id = 'user-id';                              │
│                                                          │
│ -- Shows: permission_keys (JSON array)                  │
└──────────────────────────────────────────────────────────┘

Check Role's Permissions:
┌──────────────────────────────────────────────────────────┐
│ SELECT p.permission_key, p.permission_name              │
│ FROM dynamic_role_permissions drp                       │
│ JOIN permissions p ON p.id = drp.permission_id         │
│ WHERE drp.role_id = 'role-id';                         │
└──────────────────────────────────────────────────────────┘

Audit Trail:
┌──────────────────────────────────────────────────────────┐
│ SELECT * FROM role_change_audit                         │
│ WHERE affected_user_id = 'user-id'                     │
│ ORDER BY timestamp DESC;                                │
│                                                          │
│ -- Shows all role changes for a user                    │
└──────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                    🎯 KEY DESIGN DECISIONS                              │
└─────────────────────────────────────────────────────────────────────────┘

1. ✅ Company-Scoped Roles
   - Each company gets their own roles
   - Enables customization without affecting others
   - Trade-off: More database rows (acceptable)

2. ✅ Permissions as Global Catalog
   - You control the permission list
   - Tenants can't create new permissions
   - Keeps system manageable

3. ✅ Many-to-Many User-Role Relationship
   - Users can have multiple roles
   - Sum of permissions approach
   - More flexible than single role

4. ✅ Session-Based Caching
   - Fast permission checks (<1ms)
   - Invalidated on changes
   - No external dependencies

5. ✅ Plan-Level on Permissions (not Roles)
   - Roles are company-specific
   - Permissions are global
   - Cleaner separation of concerns

┌─────────────────────────────────────────────────────────────────────────┐
│                    🚀 MIGRATION PATH                                    │
└─────────────────────────────────────────────────────────────────────────┘

Phase 1: Foundation (Week 1)
└─ Run migrations
└─ Seed permissions
└─ Create default roles

Phase 2: Backend Integration (Week 2)
└─ Update authentication
└─ Mount API routes
└─ Gradually replace requireRole() with requirePermission()

Phase 3: Frontend (Week 3)
└─ Update useUser hook
└─ Build Team & Roles UI
└─ Add permission checks to components

Phase 4: Testing & Rollout (Week 4)
└─ Test all permission flows
└─ Train support team
└─ Announce to customers
└─ Monitor and iterate

┌─────────────────────────────────────────────────────────────────────────┐
│                    🎉 SUCCESS METRICS                                   │
└─────────────────────────────────────────────────────────────────────────┘

Technical Metrics:
✅ Permission checks: 0 DB queries (cached)
✅ System extensible: Add permissions without code changes
✅ Multi-tenant isolation: 100% company-scoped
✅ Audit trail: Complete history of role changes

Business Metrics:
✅ Customer satisfaction: Customizable roles
✅ Upsell clarity: Locked features visible
✅ Support reduction: Self-service role management
✅ Competitive advantage: Enterprise-grade RBAC

┌─────────────────────────────────────────────────────────────────────────┐
│                    ✨ YOU'RE READY!                                     │
└─────────────────────────────────────────────────────────────────────────┘

This architecture delivers:
• Scalability (handles any org structure)
• Performance (cached permission checks)
• Flexibility (tenants create custom roles)
• Security (company isolation + audit trail)
• Usability (intuitive UI for non-technical users)

Welcome to world-class RBAC! 🚀
